{
  "name": "simple-app",
  "version": "1.0.0",
  "description": "Simple App",
  "main": "module.js",
  "scripts": {
    "build": "tsc && cp -r src/ dist/ && find dist -name *.ts -delete",
    "clean": "rm -rf dist/*"
  },
  "author": "A"
}
